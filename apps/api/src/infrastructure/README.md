# Storage

SQLite is the default runtime database for the current Vue + Node path. The SQL Server adapter remains available for legacy deployments.

The account/player fields are intentionally kept minimal until each gameplay domain is migrated from the legacy `Request` / `Bussiness` layer.
